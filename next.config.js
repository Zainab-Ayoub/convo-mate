// @type {import('next').NextConfig} 
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["s.gravatar.com", "sangw.in", "localhost", "picsum.photos", "lh3.googleusercontent.com"],
  },
};

module.exports = nextConfig;
